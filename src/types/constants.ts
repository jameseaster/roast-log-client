export interface IApiConstants {
  roasts: string;
  signin: string;
  baseUrl: string;
  signout: string;
  register: string;
  authenticated: string;
}

export interface IAppbarConstants {
  title: string;
}

export interface ILogConstants {
  options: string[];
}

export interface IReactQueryConstants {
  allRoasts: string;
}

export interface IGeneralConstants {
  months: string[];
}
